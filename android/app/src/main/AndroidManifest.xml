<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.employee_system"> 
    <!-- NOTE: Check that 'package' above matches your actual package name in your code -->

    <!-- ================================================= -->
    <!-- 1. PERMISSIONS (Added all required permissions)   -->
    <!-- ================================================= -->
    
    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- Location (Live Tracking) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    
    <!-- Background Services (Required for Android 10+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/> <!-- Android 14 Requirement -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/> <!-- To keep CPU running -->

    <!-- File & Gallery (Excel Upload & Profile Pics) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- Android 13+ Media Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>

    <!-- Unique Login Security -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!-- Contact & Call Logs (For Emergency Features) -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>

    <!-- ================================================= -->
    <!-- END PERMISSIONS                                   -->
    <!-- ================================================= -->

    <application
        android:label="employee_system"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true" 
        android:usesCleartextTraffic="true"> 
        <!-- requestLegacyExternalStorage helps with File Picker on Android 10 -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ================================================= -->
        <!-- 2. BACKGROUND SERVICE REGISTRATION                -->
        <!-- (Required for Live Location Tracking)             -->
        <!-- ================================================= -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />

        <!-- Flutter Meta Data -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>