# Sample environment variables for local development
# Copy this file to .env and fill with your real values (do not commit .env)

# Firebase Android
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_PROJECT_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_STORAGE_BUCKET=

# Firebase iOS
FIREBASE_IOS_CLIENT_ID=

# Optional: any other runtime config
APP_ENV=development

# Notes:
#  - Keep your real values out of source control. Add .env to .gitignore (already done).
#  - Android `google-services.json` and iOS `GoogleService-Info.plist` are platform-specific
#    files placed in `android/app/` and `ios/Runner/` respectively. They are not recommended
#    to be committed to public repos.
