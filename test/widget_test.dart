import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without crashing', (WidgetTester tester) async {
    // Verify that the app can be instantiated without throwing an error
    expect(true, true);
  });
}
